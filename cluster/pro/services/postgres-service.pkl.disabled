amends "@k8s/api/core/v1/Service.pkl"

metadata {
  name = "postgres-service"
}

spec {
  selector {
    ["app"] = "postgres"
  }
  ports {
    new { 
      protocol = "TCP"
      port = 5432
      targetPort = 5432
    }
  }
  type = "ClusterIP"
}